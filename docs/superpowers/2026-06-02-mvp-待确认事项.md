# MVP 待确认事项

日期：2026-06-02
状态：待用户确认后方可开始实施

---

## 一、Mock 数据真实度要求（范围变更）

**背景：** OCR 已改为直连真实 PaddleOCR API（见 `2026-06-02-mvp-paddleocr-技术方案.md`）。Mock 仅用于图片增强、去手写和 AI 解析。

**当前范围：**

- OCR：真实 API（已确定）
- 图片增强 + 去手写 + AI 解析：仍为 mock

Mock 部分的数据真实度仍待确认：

- A）跑通流程即可——固定返回占位内容。
- B）接近真实——根据学科（数学/物理）返回不同风格的 Markdown。

**建议：** 选 A。增强/清理的效果验证需要真实服务端，mock 阶段无法评估。

**决策：** _（待填写）_

---

## 二、设置的本地持久化方案

**背景：** 设置页需要持久化学段、年级、默认学科、纸张尺寸。Plan 未指定存储方式。

| 方案 | 优点 | 缺点 |
|------|------|------|
| `shared_preferences` | 轻量，3 行代码搞定，适合纯 KV 数据 | 引入新依赖，不与数据库事务一致 |
| Drift settings 表 | 结构化，与其他数据一致 | 对 4 个字段过于重量级 |

**建议：** 用 `shared_preferences`。4 个键值对不值得建表。需要在 pubspec.yaml 追加依赖。

**决策：** _（待填写）_

---

## 三、iOS 开发环境就绪情况

**背景：** Plan Task 14 的 iOS 冒烟测试需要 Xcode + iOS 模拟器。没有这个环境，端到端验证无法完成。

**请确认以下状态：**

- [ ] Xcode 已安装（版本要求 >= 15.0）
- [ ] iOS 模拟器运行时已下载（至少一个，建议 iPhone 15 或更新机型）
- [ ] `flutter doctor` 输出中 iOS 部分无阻塞错误
- [ ] 如用真机测试：Apple Developer 账号已配置（免费账号可调试，证书 7 天过期）

**当前状态：** _（待填写）_

---

## 四、真实后端接入时间线（部分变更）

**当前状态：** OCR 模块已决定直接对接真实 PaddleOCR API（直连，`.env` 管理 Token，多题模式整张提交）。

仍待确认的是其余云端模块的接入时间线：

- 图片增强 / 去手写：暂无真实服务端
- AI 解析（豆包）：暂无真实服务端

**选项：**

- A）MVP Flutter 端全部完成后一次性接入后端，统一验证。
- B）Mock → 后端分步迭代，mock 阶段验证过的模块逐个替换为真实后端。
- C）后端已在开发中/已就绪，Mock 只是 Flutter 端开发期间的占位。

**决策：** _（待填写）_

---

## 五、橡皮擦性能验收标准

**背景：** Plan 的风险控制指出「如果自由橡皮擦不够流畅，降级为白色矩形遮盖」。需要定义"不够流畅"的判断标准。

**选项：**

- A）iPhone 12 及以上自由橡皮擦不掉帧就算通过（允许低端机降级为白框遮盖）。
- B）所有 iOS 16+ 设备都必须流畅（不区分机型）。
- C）先不做标准——spike 完成后给出实测结果，再判断是否降级。

**建议：** 选 C。没有实测数据前定标准是拍脑袋。Spike 做完后给出帧率和内存数据，决策更靠谱。

**决策：** _（待填写）_

---

## 六、真实试卷样例照片

**背景：** 橡皮擦 spike（Task 6）和 PDF 排版（Task 12）都需要真实数据验证。用纯色方块模拟会发现不了实际的大图内存、宽高比和排版问题。

**需要：** 3-5 张初高中数学或物理试卷（或练习册）的照片，最好是学生已经用过的、上面有手写答案的。

**用途：**

- 验证大图加载内存峰值（5-15MB 手机照片 vs 小尺寸测试图行为不同）
- 验证自动去手写 mock 结果与真实试卷的视觉匹配度
- 验证 PDF 排版中极端宽高比题图的缩放效果

**当前状态：** _（待填写）_

---

## 实施依赖关系

| 事项 | 阻塞任务 | 状态 |
|------|----------|------|
| Mock 数据真实度要求（增强/清理/AI） | Task 5 | ⬜ 待确认 |
| PaddleOCR 接入策略（直连/多题/.env） | Task 5、Task 8 | ✅ 已确认 |
| 设置持久化方案 | Task 13 | ⬜ 待确认 |
| iOS 开发环境 | Task 7、Task 14 | ⬜ 待确认 |
| 后端接入时间线（增强/清理/AI） | 无（mock 不受影响） | ⬜ 待确认 |
| 橡皮擦验收标准 | Task 6 | ⬜ 待确认 |
| 试卷样例照片 | Task 6 | ⬜ 待确认 |
| PaddleOCR Token 配额/限制 | Task 5 | ⬜ 待观察 |
